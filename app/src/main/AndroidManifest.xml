<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_avatar"
        android:label="@string/app_name"
        android:name=".pro.presentation.old.viewModels.MessageApp"
        android:roundIcon="@drawable/icon_avatar"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test_Message"
        android:windowSoftInputMode="adjustResize"
        tools:targetApi="31">
       <!-- <activity
            android:name=".pro.presentation.old.chatProfileActivity.DialogChatActivity"
            android:exported="false" />
        <activity
            android:name=".pro.presentation.old.ProfileActivity.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".pro.presentation.old.chatProfileActivity.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".pro.presentation.old.loginAndRegisActivity.LogInAndRegistrationActivity"
            android:exported="false" />-->
        <activity
            android:name=".pro.presentation.activityEntry.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>